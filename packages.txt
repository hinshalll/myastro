# Myastro — Debian packages installed by Streamlit Cloud at build time.
# Render's free tier uses similar Linux images and accepts the same list.
# (One package per line — no comments next to entries; just standalone lines.)

# ── OpenCV / MediaPipe runtime ──
libgl1

# ── WeasyPrint (PDF rendering) ──
libpango-1.0-0
libpangoft2-1.0-0
libharfbuzz0b
libcairo2
libgdk-pixbuf-2.0-0
libffi-dev

# ── Fonts for PDF (Devanagari, CJK, emoji) ──
fonts-noto
fonts-noto-cjk
fonts-noto-color-emoji
