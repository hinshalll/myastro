"""features.talk — the voice companion ("talk to the Sage").

On-device speech-to-text (free) feeds this endpoint TEXT; it returns a SHORT,
warm, spoken-style reply (cheap LLM) and, when a Kokoro TTS service is wired,
the audio. Two clean modes: 'en' (English) and 'hi' (Hindi, Devanagari).
"""
