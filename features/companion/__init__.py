"""features.companion — the Companion's payoffs (Pattern Engine + The Proof).

The features that make the app feel like it *knows* you over time:

  • micro-insight — the Day-1 "mirror": today's check-in read against today's real
    transit (makes the mockup's hardcoded MIRROR real). Immediate, no history.
  • patterns     — the Pattern Engine: plain STATISTICAL correlations between your
    logged check-ins and each day's planetary state. Shows progress until enough
    data, then unlocks template-sentence insights (and stores them).
  • proof        — "Why did that happen?": given a PAST date, the dasha chapter +
    slow transits that were running then. The trust-builder.

All pure math + lookup (cost rule): no live AI. See README.md.
"""
