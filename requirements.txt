# ── FastAPI backend (api/) — the mobile-app + web-app entry point ──
fastapi
uvicorn[standard]
python-multipart
pydantic

# ── Math engine deps ───────────────────────────────────────────────
pyswisseph
geopy
timezonefinder
indic-transliteration

# ── AI engine deps ─────────────────────────────────────────────────
google-generativeai
qdrant-client
sentence-transformers
langchain-qdrant
langchain-huggingface

# ── PDF engine deps ────────────────────────────────────────────────
weasyprint
jinja2
Pillow

# ── Palm-vision deps ───────────────────────────────────────────────
opencv-python-headless
mediapipe
numpy
