streamlit
pyswisseph
geopy
timezonefinder
streamlit-local-storage
google-genai
qdrant-client
sentence-transformers
weasyprint
jinja2
indic-transliteration
Pillow
torchvision

# ── FastAPI backend (fastapi_main.py) ──────────────────────────────
# Same repo runs both:
#   • Streamlit prototype  — `streamlit run ui_streamlit/app.py`
#   • FastAPI for mobile/web  — `uvicorn fastapi_main:app`
fastapi
uvicorn[standard]
python-multipart
pydantic

# ── Palm / Vision deps (used by palm_vision module) ────────────────
opencv-python-headless
mediapipe==0.10.14
numpy

# ── LangChain integrations (used by qdrant_utils.py for vector search) ─
langchain-qdrant
langchain-huggingface
